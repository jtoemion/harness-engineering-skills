Validate flow sequence. Reply JSON: {"valid": true/false, "gate": "PASS|WARN|BLOCK", "reason": "..."}

Valid sequences (strict ordering):
  pre-task → skill → checkpoint → verification → next-task
  pre-task → skill → checkpoint → end
  pre-task → skill → end
  start → pre-task → skill → checkpoint → verification → next-task → end
  start → pre-task → skill → end

Allowed phases: start, pre-task, skill, checkpoint, verification, next-task, end

State: %STATE%